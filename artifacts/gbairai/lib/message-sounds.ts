/** Son joué quand un message arrive dans une conversation ouverte. */
export const CHAT_INCOMING_MESSAGE_SOUND = require("@/assets/sounds/sonnerie-message-entrant.wav");

/** Son joué depuis la liste des conversations (hors chat ouvert). */
export const CONVERSATION_LIST_MESSAGE_SOUND = require("@/assets/sounds/sound-background.wav");
